# Remote demo 4

Multiple machines can connect to the same remote vault concurrently.
The plugin redirects per-client state (workspace layout, recent
files) into a per-machine subtree so layouts don't fight.
