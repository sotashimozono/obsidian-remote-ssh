# Research Notes (remote)

These notes are stored on the server at `~/vault/`.

## Example entries

- Set up remote vault access via SSH
- Ran simulation batch #42 — results in `data/output/`
- Updated analysis pipeline config
